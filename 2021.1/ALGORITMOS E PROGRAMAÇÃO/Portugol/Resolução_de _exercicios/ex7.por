programa {
	funcao inicio() {
	real salario, imp, novosal
	escreva("Digite o salário-base do funcionário: ")
	leia(salario)
	imp = salario*0.10
	novosal = (salario-imp)+50
	escreva("O salário que o funcionpario tem a receber é R$ ",novosal)
	
	
	}
}
